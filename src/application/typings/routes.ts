export enum RoutesEnum {
  send = '/send',
  myAssets = '/my-assets',
  assetSelection = '/selection',
  transactions = '/transactions',
  add = '/add',
  discover = '/discover',
  myPlace = '/my-place',
  build = '/build',
  contribute = '/contribute',
  signup = '/signup',
  root = '/',
  login = '/login',
}
